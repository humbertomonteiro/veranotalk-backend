{
  "build": {
    "builder": "nixpacks",
    "config": {
      "NODE_ENV": "production"
    }
  },
  "deploy": {
    "startCommand": "node dist/main.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
